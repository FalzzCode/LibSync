<div class="form-card__header"><div><h2>Informasi akun</h2><p>Peran menentukan akses pengguna ke administrasi sistem.</p></div></div><div class="form-grid"><div class="form-group"><label for="name">Nama lengkap <span>*</span></label><input id="name" name="name" value="{{ old('name',$user?->name) }}" required @class(['is-invalid'=>$errors->has('name')])>@error('name')<small class="field-error">{{ $message }}</small>@enderror</div><div class="form-group"><label for="email">Email <span>*</span></label><input id="email" type="email" name="email" value="{{ old('email',$user?->email) }}" required @class(['is-invalid'=>$errors->has('email')])>@error('email')<small class="field-error">{{ $message }}</small>@enderror</div><div class="form-group"><label for="password">Password {{ $user ? '(opsional)' : '*' }}</label><input id="password" type="password" name="password" {{ $user ? '' : 'required' }} autocomplete="new-password">@if($user)<small class="field-hint">Biarkan kosong untuk mempertahankan password saat ini.</small>@endif @error('password')<small class="field-error">{{ $message }}</small>@enderror</div><div class="form-group"><label for="role">Peran <span>*</span></label><select id="role" name="role" required><option value="staff" @selected(old('role',$user?->role ?? 'staff') === 'staff')>Staff</option><option value="admin" @selected(old('role',$user?->role) === 'admin')>Admin</option></select></div></div><div class="form-actions"><a class="btn btn--secondary" href="{{ route('users.index') }}">Batal</a><button class="btn btn--primary" type="submit">{{ $submitLabel }}</button></div>
