@extends('layouts.app') @section('title','Kategori · Ruang Baca') @section('eyebrow','Kategori') @section('content')
<section class="page"><div class="page-header"><div><p class="eyebrow">Pengelompokan koleksi</p><h1>Kategori buku</h1><p>Susun koleksi agar setiap buku lebih mudah ditemukan.</p></div><a href="{{ route('categories.create') }}" class="btn btn--primary">+ Tambah kategori</a></div><div class="table-card"><div class="table-card__meta"><span>{{ $categories->count() }} kategori terdaftar</span></div><div class="table-wrapper"><table class="data-table"><thead><tr><th>Nama kategori</th><th>Dibuat</th><th><span class="sr-only">Aksi</span></th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->name }}</strong></td><td>{{ $category->created_at?->translatedFormat('d M Y') }}</td><td><div class="table-actions"><a class="icon-button" href="{{ route('categories.edit',$category) }}" aria-label="Edit {{ $category->name }}">✎</a><form class="inline-form js-confirm-delete" data-name="{{ $category->name }}" method="POST" action="{{ route('categories.destroy',$category) }}">@csrf @method('DELETE')<button class="icon-button icon-button--danger" aria-label="Hapus {{ $category->name }}">×</button></form></div></td></tr>@empty<tr><td colspan="3"><div class="empty-state"><span>◇</span><h2>Belum ada kategori</h2><p>Buat kategori pertama untuk mulai merapikan koleksi.</p><a class="btn btn--primary" href="{{ route('categories.create') }}">+ Tambah kategori</a></div></td></tr>@endforelse</tbody></table></div></div></section>
@endsection
